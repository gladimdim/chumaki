import 'package:flutter/material.dart';

final grey = Colors.grey[800];
final light = Colors.grey[200];
final black = Colors.black;
final mainTheme = ThemeData(
  primaryColor: black,
  buttonTheme: ButtonThemeData(
    buttonColor: light,
  ),
  backgroundColor: Colors.yellow,
);
